# bugs.md — repo defects & open-source readiness

Unlike `gaps.md` (tool-surface friction found while modeling), this file tracks defects
in the repo itself — docs, packaging, tests, hygiene. Numbered **B#**, stable, delete
when fixed.

_Seeded 2026-07-09 by an open-source-readiness audit (goal: publishable, demoable,
not embarrassing). Trivial items were fixed in the same pass: MIT LICENSE added
(the addon manifest already declared `SPDX:MIT`), `recipies/` → `recipes/`,
`.mcp.json` made portable (relative paths), personal `.codex/` config untracked,
hardcoded home path in a test docstring genericized._

_2026-07-09 (later pass): B1 (README full rewrite to the ~15-verb surface), B2 (manifest
pinned to `5.0.0`, zip rebuilt), B3 (test path via `BB_PBR_ASSET_DIR`), B4 (GitHub
Actions CI: byte-compile + import-sanity + zip build), and B6 (dropped the dead console
script) fixed and deleted. **B2 needs one operational follow-up: reinstall the rebuilt
addon in Blender** so the bumped `blender_version_min` takes effect. Only B5 (demo media)
remains — it needs a screen capture, which is a human task._

---

## B5 — No demo media

**Needs a human.** For the X.com demo: a short screen capture of an agent running
`recipes/donut/donut.md` end-to-end (it's a verified transcript-recipe), cut to ~30–60s,
plus a hero GIF/still of the finished donut near the top of the README. A headless hero
still could be generated by driving the donut recipe to its `render op=image` step — offer
that if a live Blender is attached and the human wants it.
