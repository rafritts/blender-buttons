[project]
name = "blender-buttons"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]",
]

# Launch path is `.venv/bin/python server/main.py` (see .mcp.json / README). No
# [project.scripts] entry: without a [build-system] table uv treats this as an
# unpackaged project, so an entry point would never be materialized (bugs.md B6).
